<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.module.reservation.mapper.ReservationMapper">

    <!-- ==================== 结果映射 ==================== -->
    <resultMap id="ReservationMap" type="com.example.library.module.reservation.entity.Reservation">
        <id property="id" column="id"/>
        <result property="studentId" column="student_id"/>
        <result property="seatId" column="seat_id"/>
        <result property="roomId" column="room_id"/>
        <result property="reserveDate" column="reserve_date"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="status" column="status"/>
        <result property="signToken" column="sign_token"/>
        <result property="actualSignTime" column="actual_sign_time"/>
        <result property="actualLeaveTime" column="actual_leave_time"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- ==================== BaseMapper.insert(T) 由 MyBatis Plus 自动提供 ==================== -->

    <!-- ==================== 乐观锁状态更新 ==================== -->
    <update id="updateStatusToSignedIn">
        UPDATE reservation
        SET status = 2, actual_sign_time = NOW(), version = version + 1
        WHERE id = #{id} AND status = 1 AND version = #{expectedVersion}
    </update>

    <update id="updateStatusToNoShow">
        UPDATE reservation
        SET status = 4, version = version + 1
        WHERE id = #{id} AND status = 1 AND version = #{expectedVersion}
    </update>

    <update id="updateStatusToCompleted">
        UPDATE reservation
        SET status = 3, actual_leave_time = NOW(), version = version + 1
        WHERE id = #{id} AND status = 2 AND version = #{expectedVersion}
    </update>

    <update id="updateStatusToCancelled">
        UPDATE reservation
        SET status = 5, version = version + 1
        WHERE id = #{id} AND status = 1 AND version = #{expectedVersion}
    </update>

    <!-- ==================== 查询 ==================== -->
    <select id="findPendingAndExpired" resultMap="ReservationMap">
        SELECT * FROM reservation
        WHERE reserve_date = #{reserveDate}
          AND start_time &lt;= #{deadlineTime}
          AND status = 1
        ORDER BY id
        LIMIT #{limit}
    </select>

    <select id="findInUseAndExpired" resultMap="ReservationMap">
        SELECT * FROM reservation
        WHERE status = 2
          AND end_time &lt;= #{deadlineTime}
        ORDER BY id
        LIMIT #{limit}
    </select>

    <select id="findByConditions" resultMap="ReservationMap">
        SELECT * FROM reservation WHERE 1=1
        <if test="date != null">AND reserve_date = #{date}</if>
        <if test="status != null">AND status = #{status}</if>
        <if test="roomId != null">AND room_id = #{roomId}</if>
        <if test="studentId != null">AND student_id = #{studentId}</if>
        <if test="studentNo != null">
            AND student_id IN (SELECT id FROM student WHERE student_no = #{studentNo})
        </if>
        <if test="onlyNoShow == true">AND status = 4</if>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByConditions" resultType="long">
        SELECT COUNT(*) FROM reservation WHERE 1=1
        <if test="date != null">AND reserve_date = #{date}</if>
        <if test="status != null">AND status = #{status}</if>
        <if test="roomId != null">AND room_id = #{roomId}</if>
        <if test="studentId != null">AND student_id = #{studentId}</if>
        <if test="studentNo != null">
            AND student_id IN (SELECT id FROM student WHERE student_no = #{studentNo})
        </if>
        <if test="onlyNoShow == true">AND status = 4</if>
    </select>

    <select id="existsConflict" resultType="boolean">
        SELECT COUNT(*) > 0 FROM reservation
        WHERE student_id = #{studentId}
          AND reserve_date = #{reserveDate}
          AND status IN (1, 2)
          AND start_time &lt; #{endTime}
          AND end_time &gt; #{startTime}
    </select>

    <select id="findBySeatAndTimeRange" resultMap="ReservationMap">
        SELECT * FROM reservation
        WHERE seat_id = #{seatId}
          AND reserve_date = #{reserveDate}
          AND status IN (1, 2)
          AND start_time &lt; #{endTime}
          AND end_time &gt; #{startTime}
    </select>

    <!-- 查询座位时段冲突 + 行锁（用于创建预约的锁内二次校验） -->
    <select id="findBySeatAndTimeRangeForUpdate" resultMap="ReservationMap">
        SELECT * FROM reservation
        WHERE seat_id = #{seatId}
          AND reserve_date = #{reserveDate}
          AND status IN (1, 2)
          AND start_time &lt; #{endTime}
          AND end_time &gt; #{startTime}
        FOR UPDATE
    </select>

    <!-- 按学号或手机号搜索学生，返回其待签到和使用中的预约 -->
    <select id="findByStudentNoOrPhone" resultMap="ReservationMap">
        SELECT r.* FROM reservation r
        INNER JOIN student s ON r.student_id = s.id
        WHERE (s.student_no = #{keyword} OR s.phone = #{keyword})
          AND r.status IN (1, 2)
        ORDER BY r.status, r.reserve_date DESC, r.start_time
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByStudentNoOrPhone" resultType="long">
        SELECT COUNT(*) FROM reservation r
        INNER JOIN student s ON r.student_id = s.id
        WHERE (s.student_no = #{keyword} OR s.phone = #{keyword})
          AND r.status IN (1, 2)
    </select>

</mapper>
